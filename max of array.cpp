#include<stdio.h>
int max(int a[],n)
{
	int max=a[0];
	for (int i=0;i<=n;i++)
	{
		if (max<a[i])
		{
			max=a[i];
		}
		return max;
	}
}
int main()
{
	printf("enter the size of the array \n");
	scanf("%d",)
}
